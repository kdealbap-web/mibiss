# BISS · Handoff de sesión 2026-05-16 a 2026-05-18

> **Para Claude (siguiente conversación) y Kevin.** Resumen comprimido del trabajo del fin de semana largo. Léeme primero antes de tocar código.

---

## 1 · Lo que es BISS

- **Proyecto:** "BISS · Banco de Ideas y Soluciones de Soledad" — primer libro vivo de Soledad, Atlántico.
- **Stack:** React + Vite + TypeScript + Supabase (Postgres + Auth + Edge Functions Deno) + Cloudflare Pages + Cloudflare R2 + Resend (SMTP).
- **Repo:** `https://github.com/kdealbap-web/mibiss.git` · branch `main`.
- **Local:** `D:/Desarrollo/Gitlab Personal/biss`.
- **Prod URL:** `https://mibiss.com.co`.
- **Supabase project ref:** `uicpkqwmjjrywojhwctq`.
- **Owner / PO / Dev:** Kevin De Alba P. · concejal Kevin **Balvuena** (con V).
- **Doc fuente visual:** `Design/HANDOFFv2.0.md` (gitignored, vive local). Folder con HTMLs/CSS de prototipo en `Design/`.

---

## 2 · Estado de producción al cierre del 2026-05-18

- **HEAD:** `8975a5d` (sincronizado con `origin/main`).
- **Commits acumulados sobre baseline `c0491b9`:** 36.
- **TSC:** exit 0. **`vite build`:** OK (1787 modules transformed).
- **Working tree:** limpio.
- **Edge functions desplegadas (4 activas):**
  - `r2-presign` — presigned PUT a R2 para uploads.
  - `invitar-editor` — Supabase Admin API + insert `usuarios_cms`.
  - `sitemap-xml` — XML dinámico de casos + capítulos.
  - `notify-email` — Resend SMTP wired a triggers DB.
- **Migraciones DB aplicadas a prod (12):** `db/14` → `db/25`. Detalle abajo.
- **Cloudflare Pages:** auto-deploy desde `main`. Acción humana opcional pendiente: `VITE_CF_ANALYTICS_TOKEN` para Cloudflare Web Analytics geo/devices.
- **CORS R2:** ✅ aplicado por Kevin el 2026-05-18.

---

## 3 · Mapa rápido del repo

```
biss/
├── db/                  · migraciones SQL (00 a 25). Aplicadas vía `supabase db query --linked --file ...`
├── docs/                · este handoff + smoke test resultados Sprint C
├── backend/
│   ├── .env.secrets     · gitignored. R2 + Supabase keys + Resend.
│   └── edge-functions/  · código Deno de las edge functions.
├── supabase/functions/  · copia que el CLI usa para `supabase functions deploy`. Gitignored.
├── frontend/
│   ├── public/
│   │   ├── _redirects   · /sitemap.xml → edge fn + SPA fallback
│   │   └── robots.txt
│   └── src/
│       ├── App.tsx · main.tsx · router.tsx
│       ├── context/SessionContext.tsx · FlowDrawer.tsx
│       ├── hooks/       · useMiCuenta · useCasos · useBarrios · useTrackPageview · useR2Upload · etc.
│       ├── lib/         · supabase · config · format · queryClient
│       ├── styles/      · tokens · base · components · home · admin · etc.
│       ├── components/
│       │   ├── ui/      · Button · SearchInput · FilterChip · Select · Modal · Toggle · BarrioAutocomplete · Drawer · UserChip
│       │   ├── layout/  · Navbar · NavMobile · AdminLayout · AdminSidebar · AdminTopbar · Footer · UserChip
│       │   ├── flows/   · FlowReportar · FlowIngresar · FlowTestimonio · FlowApadrinar · FlowShell
│       │   ├── map/     · BissMap · MapDrawer
│       │   ├── brand/   · BissLogo · BissMark
│       │   └── admin/   · BarrioMapPicker
│       └── pages/
│           ├── Splash · Home · Capitulo · Caso · Categoria · Barrios · MiCuenta
│           ├── Login · Recuperar · RecuperarNuevaContrasena
│           └── admin/   · Dashboard · Solicitudes · CasosLista · CasoEdit · TestimoniosAdmin · MapaBarrios · PadrinosAdmin · Usuarios · Metricas · Ajustes · BarriosLista · BarrioEditor · CapitulosLista · CapituloEditor
└── PORT_NOTES.md        · log canónico (lee antes de cualquier cambio)
```

---

## 4 · Sprints completados (cronología)

### Sprint D (2026-05-16/17) · 13 bloques

Fix bugs prod (vista vieja) + delta visual completo del HANDOFF v2.0. Resumen:

- **D-B1** `db/14`: DROP+CREATE `v_capitulos_publicos` con `casos_progreso` (fix BUG-C1 del smoke test).
- **D-B2**: Auth email-only · Login sin tabs · `/recuperar` + `/recuperar/nueva-contrasena` · `useMiRol` nuevo.
- **D-B3**: UI primitives `<SearchInput />`, `<FilterChip />`, `<Select />`.
- **D-B4**: Splash mobile responsive + tagline con `.accent` highlight + cancel auto-redirect.
- **D-B5**: Admin sidebar drawer mobile `<900px` con burger + overlay.
- **D-B6**: `/admin/ajustes` con 6 secciones (perfil, seguridad, notificaciones, flujos, integraciones, exportar). `db/15` tabla `config_app`.
- **D-B7**: `/admin/barrios` CRUD + editor Leaflet con marker arrastrable + Nominatim rate-limited.
- **D-B9**: Flows padding mobile ≤640px.
- **D-B10**: `useFocusTrap` + Drawer integration + axe docs.
- **D-B11**: `SOLEDAD_CENTER` y `SOLEDAD_BOUNDS` en `lib/config.ts`.
- **D-B13**: Cleanup + docs + `R2_PUBLIC_BASE_URL → R2_PUBLIC_URL`.

(D-B8 lucide migration ya estaba hecho · D-B12 auditoría diferida a Sprint E+.)

### Sprint E (2026-05-17) · 3 bloques

Cierre ciclo ciudadano → caso publicado.

- **E1**: Upload R2 real en FlowReportar. `db/16` añade `solicitudes_caso.fotos_urls text[]`. Hook `useR2Upload`.
- **E2**: `/admin/solicitudes` con preview fotos + aprobar/rechazar/duplicada. `db/17` actualiza RPC `aprobar_solicitud` para copiar fotos a `multimedia_casos`. Vista `v_solicitudes_pendientes` enriquecida.
- **E3**: `/admin/capitulos` lista + editor (descripción, imagen portada R2, geocerca GeoJSON, toggle activo respeta `chk_capitulos_activo_completo`).

### Sprint F (2026-05-17/18) · 4 bloques + smoke audit del HANDOFF v2.0

Bugs encontrados y corregidos en commit `42d3f5c`:
- BissMap no recibía filtros catFilter/stateFilter.
- Click en barrio del panel lateral muerto (`<a>+preventDefault`).

Y commit `25efb56` (fix mobile específico):
- Tiles grises Leaflet → `invalidateSize()` triple.
- Pines mal ubicados → fallback `barrios.coord_lat/coord_lng`.
- "Un segundo" overlay → skeleton.
- Mapa altura responsive (420px mobile / 520 tablet / 600 desktop).

Bloques entregables:
- **F1**: `/admin/casos` lista CRUD con KPIs + edad color-coded.
- **F2**: `db/18` tabla `caso_notas_internas` + RPC `reabrir_caso`. UI en CasoEdit.
- **F3**: `db/19` RPCs `suspender_ciudadano` + `reactivar_ciudadano`. Edge function `invitar-editor`. Modal primitive `<Modal />` nuevo. UI en `/admin/usuarios`.
- **F4**: Edge functions `sitemap-xml` + `notify-email` desplegadas. `_redirects` con rewrite. Secrets de Resend configurados en Supabase.

### Sprint G (2026-05-18) · ciclo ciudadano

- **G1**: `/mi-cuenta` perfil editable (form + UPDATE ciudadanos via RLS `update_self` + cambio de email via `auth.updateUser`).
- **G2**: `db/20` RPC `mis_padrinazgos()` SECURITY DEFINER que matchea `padrinos.contacto_privado_email = auth.email`. Tab Padrinazgos vinculados.
- **G3**: Historial inline por caso en `/mi-cuenta` (expand fila → últimas 3 actualizaciones).

### Sprint H (2026-05-18) · backend automatizado + admin moderation

- **H1**: `db/21` pg_net habilitado + helper `enviar_notificacion()` + triggers AFTER INSERT/UPDATE en `solicitudes_caso` + RPCs `aprobar_solicitud` y `cambiar_estado_caso` ahora disparan emails automáticos vía `notify-email`.
- **H2**: `/admin/testimonios` reescrita con FilterChip + SearchInput + `useTestimoniosAdmin` + `useAprobarLote` (bulk).
- **H3**: `/admin/padrinos` refactor con FilterChip + Modal primitive + CSV export.

### Sprint I (2026-05-18) · métricas reales

- **I1**: `db/22` tabla `web_visits` (privacy-friendly · sin IP, sin cookies) + RLS anon-INSERT/cms-SELECT + 3 vistas KPI. Hook `useTrackPageview` montado en App.tsx (excluye `/admin/*`).
- **I2**: `/admin/metricas` reescrita con queries reales (KPIs visitas, chart semanal, top paths, KPIs operativos, chart abiertos vs resueltos por mes, top categorías, top barrios).
- **I3**: Snippet Cloudflare Web Analytics opcional via `VITE_CF_ANALYTICS_TOKEN`.

### Lote adicional (2026-05-18 fin de día) · feedback intensivo de Kevin

Múltiples mejoras pedidas tras smoke test móvil:
- **`6a50fcd`**: Indicador visitas público en Home (pill bajo stats con `db/23` vista pública).
- **`15dc40d`**: 4 bugs (admin sidebar mobile, mapa overlay, pines barrio real, "Hola, vecino" → nombre real).
- **`3763477`**: 7 bugs mobile (drawer mapa fixed, overflow, perfil sidebar dinámico, navbar logueado).
- **`7545bd2`**: 8 bugs (RLS testimonios `db/24`, UserChip primitivo, compartir caso, página `/categoria/:codigo`, panel lateral filtra con casos + modal sin-casos, cards apadrinar visuales).
- **`8975a5d`**: 7 bugs (filtros mapa restaurados, página `/barrios`, `BarrioAutocomplete`, tiers padrino `db/25`, FlowTestimonio error real).

---

## 5 · Migraciones DB aplicadas a prod

Todas las migraciones se aplicaron a `uicpkqwmjjrywojhwctq` (prod) con `npx supabase db query --linked --file <archivo>`. NO usar `supabase db push` porque las migraciones no están en formato CLI estándar.

| # | Archivo | Razón | Fecha |
|---|---|---|---|
| 14 | `db/14-fix-v-capitulos-publicos.sql` | Fix drift `casos_gestion → casos_progreso`. BUG-C1 cerrado | 2026-05-17 |
| 15 | `db/15-config-app.sql` | Tabla `config_app` key/value + RLS + 9 seeds | 2026-05-17 |
| 16 | `db/16-fotos-solicitudes.sql` | `solicitudes_caso.fotos_urls text[]` | 2026-05-17 |
| 17 | `db/17-aprobar-solicitud-fotos.sql` | RPC `aprobar_solicitud` copia fotos a `multimedia_casos` + vista `v_solicitudes_pendientes` enriquecida | 2026-05-17 |
| 18 | `db/18-notas-internas-y-reabrir.sql` | Tabla `caso_notas_internas` + RPC `reabrir_caso` | 2026-05-17 |
| 19 | `db/19-suspender-ciudadano.sql` | RPCs `suspender_ciudadano` y `reactivar_ciudadano` | 2026-05-17 |
| 20 | `db/20-mis-padrinazgos.sql` | RPC `mis_padrinazgos()` SECURITY DEFINER | 2026-05-18 |
| 21 | `db/21-notify-cableado.sql` | `pg_net` + `enviar_notificacion()` + triggers + RPCs actualizadas | 2026-05-18 |
| 22 | `db/22-web-visits.sql` | Tabla `web_visits` + 3 vistas KPI | 2026-05-18 |
| 23 | `db/23-visitas-publicas.sql` | Vista pública `v_visitas_publicas_mes` (GRANT anon) | 2026-05-18 |
| 24 | `db/24-rls-testimonios-relax.sql` | RLS testimonios sin `verificado_email`. Solo pide auth_user_id match | 2026-05-18 |
| 25 | `db/25-padrinos-tier.sql` | Enum `padrino_tier` (bronce/plata/oro) + columna en `padrinos` | 2026-05-18 |

---

## 6 · Edge functions activas en prod

| Function | Endpoint | JWT | Estado |
|---|---|---|---|
| `r2-presign` | `https://uicpkqwmjjrywojhwctq.supabase.co/functions/v1/r2-presign` | Verify | ACTIVE v3 |
| `invitar-editor` | `.../functions/v1/invitar-editor` | Verify | ACTIVE v3 |
| `sitemap-xml` | `.../functions/v1/sitemap-xml` | No-verify | ACTIVE v1 |
| `notify-email` | `.../functions/v1/notify-email` | No-verify | ACTIVE v1 |

Secrets configurados en Supabase (Vault): `RESEND_API_KEY`, `EMAIL_FROM=BISS <hola@mibiss.com.co>`, `SITE_URL=https://mibiss.com.co`, R2 keys, SUPABASE_* keys.

---

## 7 · Bugs reportados pendientes (al cierre del 2026-05-18)

### 🔴 Crítico (data, no código)
- **Pines en aeropuerto/ciénaga** — el código siempre usa `barrios.coord_lat/coord_lng`. Las coords mal son **data** que Kevin debe corregir desde `/admin/barrios`:
  - **Los Almendros** `10.913, -74.787` → cae cerca del Aeropuerto Cortissoz. Mover.
  - **Bello Horizonte** `10.880, -74.785` → cae cerca del río Magdalena. Mover.
  - Centro y Manuela Beltrán están OK.
  - **Acción Kevin:** entrar a `/admin/barrios/los-almendros/editar` y arrastrar el marker al lugar real. Idem Bello Horizonte. Cualquier otro que veas mal.

### 🟡 Confirmar tras deploy de `8975a5d`
- Testimonio: el mensaje "Necesitas verificar tu email" debería estar resuelto (RLS db/24 + cliente con mejor manejo). **Si vuelve a aparecer**, abrir DevTools console para leer `[testimonio] INSERT error {...}` y mandar el `code`.
- Splash icono blanco — Kevin lo reportó pero no me dio captura para identificar cuál. Pendiente revisar imagen.

### 🟢 Mejoras nice-to-have (no urgentes)
- Aplicar `BarrioAutocomplete` también en FlowReportar paso 3 y en admin/Usuarios. (Solo lo apliqué a FlowIngresar.)
- Tier de padrinos: hoy es **opcional** y no se muestra en `/admin/padrinos` ni en la tarjeta pública del padrino. Falta wiring de display.
- Editor inline en `/admin/solicitudes` antes de aprobar (botón existe disabled "Sprint F").
- Auditoría admin `/admin/auditoria` (diferida desde Sprint D).
- OG image dinámica edge function.
- 2FA TOTP.
- email_log con webhooks Resend.
- Importar CSV barrios.
- Eliminar `verificado_sms` legacy de DB (deprecated desde Sprint C).

---

## 8 · Acciones humanas pendientes

| # | Acción | Quién | Cuándo |
|---|---|---|---|
| 1 | Corregir coords de **Los Almendros** y **Bello Horizonte** desde `/admin/barrios/:slug/editar` (arrastrar marker) | Kevin | Apenas posible |
| 2 | Smoke test e2e desde móvil real fuera de FortiClient: reportar caso con foto → ver email "recibida" → aprobar en admin → ver email "aprobada" → caso público con foto | Kevin | Cuando tenga datos móviles |
| 3 | Activar Cloudflare Web Analytics (opcional · `VITE_CF_ANALYTICS_TOKEN` en Pages env vars) | Kevin | Opcional |
| 4 | Cron retención `DELETE FROM web_visits WHERE ts < now() - interval '90 days'` semanal | Kevin | Post-launch |
| 5 | Verificar Sitemap `https://mibiss.com.co/sitemap.xml` responde XML | Kevin | Post-deploy |

---

## 9 · Convenciones del código (mantenlas)

- **Apellido del concejal:** "**Balvuena**" con V. Hay 5 matches verificados. Cero matches de "Balbuena". NO romper.
- **Auth:** email-only desde Sprint C. NUNCA pedir SMS/celular/Twilio. Excepción: `ciudadanos.telefono_celular` es campo opcional de contacto secundario, NO eliminar.
- **Coords de casos:** SIEMPRE usar `barrios.coord_lat/coord_lng` con jitter determinista por `caso.id`. NUNCA usar `casos.lat/lng` directamente (son aproximadas y caen mal).
- **RLS testimonios:** solo requiere `auth_user_id` match. NO restaurar el require de `verificado_email`.
- **Migraciones:** numeradas secuencialmente. Aplicar con `npx supabase db query --linked --file db/NN-*.sql`.
- **Edge functions:** código vive en dos lugares:
  - `backend/edge-functions/<name>/index.ts` — fuente versionada en repo.
  - `supabase/functions/<name>/index.ts` — copia para el CLI (gitignored). Copiar manualmente antes de `supabase functions deploy`.
- **Sin emojis** en código salvo que el usuario lo pida. Sin docstrings largos. Solo comentarios de WHY no obvios.
- **CSS:** tokens en `tokens.css`, primitivos en `components.css` dentro de `@layer components`. Reglas mobile críticas (como admin-sidebar drawer) van FUERA del @layer con `!important` para ganar especificidad.
- **Zona segura HANDOFF v2.0 §11.2:** no tocar mapa filtros, splash 3D, sidebar admin orden, marker arrastrable admin-barrios, tokens.
- **PORT_NOTES.md:** appendear, nunca sobrescribir. Cada sprint tiene sección `## Sprint X — Cierre (YYYY-MM-DD)`.

---

## 10 · Cómo retomar mañana (cheatsheet)

```bash
# 1. cwd
cd "D:/Desarrollo/Gitlab Personal/biss"

# 2. confirmar sync
git status
git log --oneline -5

# 3. health check
cd frontend && npx tsc --noEmit && npx vite build && cd ..

# 4. Supabase (si necesitas DB)
npx -y --quiet supabase functions list
npx -y --quiet supabase db query --linked "SELECT 1;"

# 5. ver pendientes en PORT_NOTES
cat PORT_NOTES.md | grep -A 3 "Decisiones pendientes\|Acciones humanas\|pendiente"
```

**Files que mira PRIMERO al retomar:**
- `PORT_NOTES.md` — log canónico cross-sprint.
- `docs/HANDOFF-2026-05-18.md` — este archivo.
- `Design/HANDOFFv2.0.md` — fuente visual.

**Comando rápido para aplicar nueva migración:**
```bash
npx -y --quiet supabase db query --linked --file db/26-tu-archivo.sql
```

**Comando rápido para desplegar edge function:**
```bash
# 1. copia el código al folder del CLI
cp backend/edge-functions/<name>/index.ts supabase/functions/<name>/index.ts
# 2. deploy
npx -y --quiet supabase functions deploy <name> --no-verify-jwt  # o sin flag si requiere auth
```

---

## 11 · Tareas naturales para mañana (sugeridas)

En orden de impacto:

1. **Verificar pines en mapa post-corrección de coords** — si Kevin corrigió Los Almendros y Bello Horizonte desde admin, refrescar Home y confirmar que se ven bien.
2. **Aplicar `BarrioAutocomplete` en FlowReportar paso 3** (donde hoy el usuario tipea ubicación libre) y en `/admin/usuarios` cuando se filtre por barrio.
3. **Display de tier en `/admin/padrinos` y en cards públicas de caso** — badge color Bronce/Plata/Oro al lado del nombre del padrino.
4. **Smoke test e2e narrado** — Kevin reporta el ciclo completo y arreglamos lo que aparezca.
5. **Eliminar deprecation** `verificado_sms` con `db/26` (`ALTER TABLE ciudadanos DROP COLUMN verificado_sms`). Requiere verificar que ningún código lo lee.

---

## 12 · Memoria personal de Claude

Para futuras conversaciones, recuerda:

- Kevin trabaja desde **red corporativa de SuperGiros Atlántico** (FortiClient bloquea `mibiss.com.co` y `media.mibiss.com.co`). Para smoke tests reales él usa datos móviles o red doméstica.
- Kevin valora **velocidad sobre forma**: prefiere commits que avancen producto antes que pulir cada detalle. Igual quiere calidad — solo no pierde tiempo en bikeshedding.
- Cuando hay 2 caminos razonables en una decisión arquitectónica, **no inventes** — anota en PORT_NOTES bajo `⚠️ Decisiones pendientes Sprint X` y avanza con la opción menos invasiva.
- Kevin usa Claude Code en remote control desde el móvil mientras trabaja autónomo de fondo. Las respuestas largas no son problema; los reportes con bullets y tablas le ayudan a leer rápido.

---

**Fin del handoff. Sesión cerrada con HEAD `8975a5d` sincronizado a origin.**
**— Claude Opus 4.7 · 2026-05-18**
